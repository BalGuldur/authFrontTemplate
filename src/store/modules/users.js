const state = {
  users: {}
}

const getters = {
  users: state => state.users
}

const actions = {
  fetchUsers () {
  }
}

const mutations = {
}

export default {
  state,
  getters,
  actions,
  mutations
}
